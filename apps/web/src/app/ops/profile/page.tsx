import { WholesalerProfileSettings } from "@/components/ops/WholesalerProfileSettings";

export const metadata = { title: "Profile Settings — Sentinel Ops" };

export default function ProfilePage() {
  return <WholesalerProfileSettings />;
}
