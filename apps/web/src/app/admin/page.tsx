import { AdminDashboard } from "@/components/ops/AdminDashboard";

export const metadata = { title: "Super Admin Console — Market Buddy" };

export default function StandaloneAdminPage() {
  return <AdminDashboard />;
}
